#
# See all options:
#
# npx openapi-generator-cli help generate
#
# Note: you must convert the CLI arguments from kebab case to camel case.
#

#
# See all options for 'generatorName' with:
#
# npx openapi-generator-cli list
#
generatorName: typescript-fetch
outputDir: src
inputSpec: ../upload/api/src/common/routing/generated/openapi-spec-v3-sdk.json
templateDir: generator

#
# See all options with:
#
# npx openapi-generator-cli config-help -g typescript-fetch
#
# Some options are also taken from this config file's root fields (e.g. GitRepoId). See this file's header for
# instructions on listing all the available root options.
#
additionalProperties:
  withoutRuntimeChecks: true

#
# See all supported template files here:
#
# https://github.com/OpenAPITools/openapi-generator/tree/v6.2.1/modules/openapi-generator/src/main/resources/typescript-fetch
#
files:
  modelEnum.mustache:
    templateType: Model
    destinationFilename: .ts
  modelGeneric.mustache:
    templateType: Model
    destinationFilename: .ts
  apis.mustache:
    templateType: API
    destinationFilename: .ts
  runtime.mustache:
    templateType: SupportingFiles
    destinationFilename: runtime.ts
  index.mustache:
    templateType: SupportingFiles
    destinationFilename: index.ts
  utils/ChunkedStream.ts: {}
  utils/index.ts: {}
  utils/Model.ts: {}
  utils/UploadManager.ts: {}
